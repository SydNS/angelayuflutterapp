import 'package:flutter/material.dart';
//at every end of a widget add a comma
void main() {
  runApp(MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: Text("data"),
        ),
      )

  ));
}
